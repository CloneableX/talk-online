<li>
  <div class="p-4 mb-4 max-w-prose text-sm text-blue-700 bg-blue-100 rounded-lg dark:bg-blue-200 dark:text-blue-800" role="alert">
    <p>
      <span class="font-bold"><%= message.sender_name %>:</span>
      <%= message.content %>
    </p>
    <p class="font-light text-xs text-gray-600">
      at <%= message.created_at.strftime('%F %T') %>
    </p>
  </div>
</li>
